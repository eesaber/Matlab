%% The code implement decomposition by using non-negative matrix factorization

%% Simulated data
C = random(1); % Coherent target space
A = random(1); % Assembly matrix 
X = random(1); % Spatial distribution matrix
